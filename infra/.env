STACK_NAME=fastapp
DOMAIN=example.com

EMAIL=admin@example.com
USERNAME=admin
HASHED_PASSWORD=$2y$10$.......  # htpasswd hash

# Postgres
POSTGRES_USER=appuser
POSTGRES_PASSWORD=supersecret
POSTGRES_DB=appdb

# Backend Images
DOCKER_IMAGE_BACKEND=fastapi-backend
DOCKER_IMAGE_FRONTEND=fastapi-frontend
TAG=latest



If you want next, we can:

add JWT revocation (jti)

add tenant-aware auth

add permission middleware

standardize 403 vs 401

wire auth into WebSockets


Add @public_route decorator (later)

Instead of safe_endpoints list, later you can do:

@public_route
@route("post", "/login")


But your current approach is solid.